<div class="row">
	<div class="col-lg-12">
		<% @projects.each do |project| %>
			<div class="col-lg-3 bg-primary custom-style">
				<p>
					<span><b> Name: </b></span>
					<%= project.name %>
				</p>
				<p>
					<span><b> Category: </b></span>
					<%= project.category%>
				</p>
				<p>
					<span><b> Framework: </b></span>
					<%= project.framework %>
				</p>
				<!-- <p>
					<%= image_tag project.attachments.first.photo.url(:medium
					) if project.attachments.present? %>
				</p> -->
				<!--<%= link_to 'Destroy', departement_project_path(project), method: :delete, :class => "btn btn-danger btn-md", data: { confirm: 'Are you sure?' } %> -->
			</div>
		<% end %>
	</div>
</div>